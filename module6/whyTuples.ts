// Most notable use of tuples will be if we are ever working with a CSV file

const carSpecs: [number, number] = [400, 3354];

// Purpose for these numbers? we can not understand what those numbers say
// we know that they are two numbers and that is pretty much it

const carStats: {
  horsePower: number;
  weight: number;
} = {
  horsePower: 400,
  weight: 3354,
};
